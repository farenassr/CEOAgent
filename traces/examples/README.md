# Sanitized Trace Examples

This directory stores scrubbed trace examples for harness reviews and future
eval replay. Files must not include raw customer text, prompts, completions,
tokens, provider credentials, or connection strings.

Use `sample-whatsapp-ingestion.json` as the minimal shape for future examples.
